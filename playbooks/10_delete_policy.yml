---
- name: Delete only the explicitly identified lab policy
  hosts: fortigates
  gather_facts: false
  vars_files:
    - ../vars/lab.yml
  roles:
    - role: fortigate_policy
      policy_state: absent
