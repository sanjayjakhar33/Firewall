# AWX Inventory

Create inventory `FortiGate-Lab` in organization `FortiGate Lab`. Add host `fortigate01` to group `fortigates`.

Use the repository inventory as the source. Do not enter the API token in host variables. Supply runtime values through an AWX credential/environment injection and the local variable contract documented in `inventories/lab/hosts.yml`.
