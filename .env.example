# Copy to .env locally. Replace placeholders; never commit the populated file.
FORTIGATE_HOST=FORTIGATE_HOST_OR_IP
FORTIGATE_VDOM=root
FORTIGATE_API_TOKEN=REPLACE_LOCALLY
FORTIGATE_VALIDATE_CERTS=false